generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model programs {
  id        Int        @id @default(autoincrement())
  programID String     @unique(map: "programs_programid_unique") 
  name      String     
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
}
